<p id="notice"><%= notice %></p>
<p>
  <b> Movies by this actor </b>
    <% @actor.movies.each do |movie| %>
      <ul>
        <li> <%= movie.title %></li>
      </ul>
      <% end %>
</p>

<%= link_to 'Edit', edit_actor_path(@actor) %> |
<%= link_to 'Back', actors_path %>
